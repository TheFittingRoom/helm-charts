apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "avatar-worker.fullname" . }}-configmap
data:
  ENVIRONMENT: {{ .Values.environment }}
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | default "" | quote }}
  {{- end }}
