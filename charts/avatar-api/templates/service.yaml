apiVersion: v1
kind: Service
metadata:
  name: {{ include "avatar-api.fullname" . }}
  labels:
    {{- include "avatar-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: app
      protocol: TCP
      name: app
  selector:
    {{- include "avatar-api.selectorLabels" . | nindent 4 }}
