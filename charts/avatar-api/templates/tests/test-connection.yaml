apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "avatar-api.fullname" . }}-test-connection"
  labels:
    {{- include "avatar-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "avatar-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
