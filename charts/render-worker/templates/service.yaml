apiVersion: v1
kind: Service
metadata:
  name: {{ include "render-worker.fullname" . }}
  labels:
    {{- include "render-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: app
      protocol: TCP
      name: app
  selector:
    {{- include "render-worker.selectorLabels" . | nindent 4 }}
    role: api
