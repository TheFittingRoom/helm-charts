apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vto.fullname" . }}-test-connection"
  labels:
    {{- include "vto.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vto.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
