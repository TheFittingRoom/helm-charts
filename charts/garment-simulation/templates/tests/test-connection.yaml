apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "garment-simulation.fullname" . }}-test-connection"
  labels:
    {{- include "garment-simulation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "garment-simulation-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
