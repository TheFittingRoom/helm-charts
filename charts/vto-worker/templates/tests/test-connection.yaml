apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vto-worker.fullname" . }}-test-connection"
  labels:
    {{- include "vto-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vto-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
