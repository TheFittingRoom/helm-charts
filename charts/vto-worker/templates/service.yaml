apiVersion: v1
kind: Service
metadata:
  name: {{ include "vto-worker.fullname" . }}
  labels:
    {{- include "vto-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: app
      protocol: TCP
      name: app
  selector:
    {{- include "vto-worker.selectorLabels" . | nindent 4 }}
