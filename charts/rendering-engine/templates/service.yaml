apiVersion: v1
kind: Service
metadata:
  name: {{ include "rendering-engine.fullname" . }}
  labels:
    {{- include "rendering-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: app
      protocol: TCP
      name: app
  selector:
    {{- include "rendering-engine.selectorLabels" . | nindent 4 }}
