apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "avatar-worker.fullname" . }}-test-connection"
  labels:
    {{- include "avatar-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "avatar-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
