apiVersion: v1
kind: Service
metadata:
  name: {{ include "measure-worker.fullname" . }}
  labels:
    {{- include "measure-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: app
      protocol: TCP
      name: app
  selector:
    {{- include "measure-worker.selectorLabels" . | nindent 4 }}
