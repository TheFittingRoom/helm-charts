apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "measure-worker.fullname" . }}-test-connection"
  labels:
    {{- include "measure-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "measure-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
