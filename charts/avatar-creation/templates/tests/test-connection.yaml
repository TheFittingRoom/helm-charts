apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "avatar-creation.fullname" . }}-test-connection"
  labels:
    {{- include "avatar-creation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "avatar-creation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
